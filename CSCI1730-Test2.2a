#include <iostream>
using namespace std;

void charline (char a = '*', int num = 40);

int main()
{
    charline ();
    charline('#');
    charline('$',10);
    return 0;

}
void charline (char a, int num)
{
    for (int i=0; i<num; i++)
    {
        cout <<a;
    }
    cout<<endl;
}
